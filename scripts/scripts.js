function btn() {
  var length1 = document.getElementById("hypotenus");
  var length2 = document.getElementById("adjacent");
  var length3 = document.getElementById("opposite");

  var length4 = parseInt(hypotenus);
  var length5 = parseInt(adjacent);
  var length6 = parseInt(opposite);
document.getElementById("answer").innerHTML=add;

if (hypotenus === adjacent && hypotenus === opposite && adjacent === opposite) {
  else if (hypotenus === adjacent || hypotenus === opposite || adjacent === opposite) {
  };
  else if (hypotenus !== adjacent || hypotenus !== opposite || adjacent !== opposite )
  };
else ((hypotenus + adjacent) <= opposite || (hypotenus + opposite) <= adjacent || (adjacent + opposite) <= hypotenus) {
  };
};
